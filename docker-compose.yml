version: '3.8'
services:
  app:
    build: ./app
    deploy:
      replicas: 3
    depends_on:
      - redis
  redis:
    image: redis:alpine
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - app
